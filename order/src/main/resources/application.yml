spring:
  application:
    name: order
  config:
    import: optional:configserver:http://localhost:8888,optional:file:.env[.properties]

#logging:
#  file:
#    name: logs/${spring.application.name}.log
#  logback:
#    rollingpolicy:
#      max-file-size: 10MB
#      max-history: 7 # Days available